#include<stdio.h>

    void even(int n)
    {
        if(n!=1&&n!=0)
        {
            printf("%d ",n);
            n-=2;
            even(n);
        }
    }

int main()
{
    int n;
    printf("FROM : ");
    scanf("%d",&n);
    even(n);
    return 0;
}
